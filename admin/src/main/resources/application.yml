server:
  port: 9001

spring:
  application:
    name: admin

eureka:
  client:
    service-url:
      # Eureka????????
      # ?????????????????? http://??/eureka/ ??
      defaultZone: http://localhost:8080/eureka/
